name: Release

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

    - name: Set up Go 1.x
      uses: actions/setup-go@v2
      with:
        go-version: ^1.13
      id: go

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    - name: Get dependencies
      run: |
        cd easyDoser-server/
        go get -v -t -d ./...
        if [ -f Gopkg.toml ]; then
            curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
            dep ensure
        fi
    - name: Build
      run: cd easyDoser-server/ && go build .
    - name: Upload binaries to release
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file: easyDoser-server
        asset_name: easyDoser-server
        tag: 0.8
    # - name: cp  
    #   run: cp easyDoser-server/easyDoser-server ../output 
    - uses: actions/checkout@v2
    - name: Use Node.js 14.x
      uses: actions/setup-node@v1
      with:
        node-version: 14.x
    - run: cd easyDoser-frontend && rm -rf node_modules && rm -rf package-lock.json && npm install
    - run: cd easyDoser-frontend && npm run build --if-present
    - name: make  output dir 
      run: mkdir output && mkdir output/frontend
    - name: copy output
      run: cp -r easyDoser-frontend/build output/frontend
    - uses: montudor/action-zip@v0.1.0
      with:
        args: zip -qq -r release.zip output
    - name: ls
      run: ls output
    - name: ls
      run: ls easyDoser-server
    - name: ls
      run: ls 
    - name: Upload binaries to release
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file: release.zip
        asset_name: easyDoser-frontend.zip
        tag: 0.8